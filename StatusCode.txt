What are HTTP Status Codes?

Whenever your frontend (React, browser, Postman) sends a request to a server (Node.js, Express, API), the server responds with:

* A status code (number)
* A message/body (optional)

The number alone already tells a story.

---

Status codes are grouped by their **first digit**:

| Range   | Meaning       | Mood                  |
| ------- | ------------- | --------------------- |
| **1xx** | Informational | “Hold on…”            |
| **2xx** | Success       | “All good 😎”         |
| **3xx** | Redirection   | “Go there instead 👉” |
| **4xx** | Client Error  | “You messed up 😅”    |
| **5xx** | Server Error  | “I messed up 😭”      |

---

## 1xx – Informational (Rare in daily dev life)

Mostly used internally by browsers.

* **100 Continue** – Server says: “Okay, send the rest.”

You’ll almost never manually handle these.

---

## 2xx – Success Codes (Your best friends 🫂)

These mean the request worked.

### Common Ones:

* **200 OK**
  Everything went perfectly.
  Example:

  ```ts
  res.status(200).json(users);
  ```

* **201 Created**
  New resource successfully created.
  Example: user signup, product added.

  ```ts
  res.status(201).json({ message: "User created" });
  ```

* **204 No Content**
  Success, but nothing to return.
  Example: delete operation.

💡 Rule of thumb:

* GET → 200
* POST (create) → 201
* DELETE → 204

---

## 3xx – Redirection Codes

The server says: “The thing you want lives somewhere else.”

* **301 Moved Permanently** – Old URL is gone forever.
* **302 Found** – Temporary redirect.
* **304 Not Modified** – Use cached version.

Mostly handled by browsers and frameworks, not daily API logic.

---

## 4xx – Client Errors (Your fault 👀)

The request was bad, incomplete, or unauthorized.

### Most Important Ones:

* **400 Bad Request**
  Invalid input.

  ```ts
  if (!email) {
    res.status(400).json({ error: "Email required" });
  }
  ```

* **401 Unauthorized**
  No valid authentication (token missing/invalid).

* **403 Forbidden**
  Authenticated, but **not allowed**.

  > “I know who you are, but you can’t do this.”

* **404 Not Found**
  Resource doesn’t exist.

  ```ts
  res.status(404).json({ error: "User not found" });
  ```

* **409 Conflict**
  Data conflict. Example: email already exists.

🧠 Interview tip:
401 ≠ 403

* **401** → not logged in
* **403** → logged in but blocked

---

## 5xx – Server Errors (Backend crying 💥)

The request was fine, but the server failed.

* **500 Internal Server Error**
  Something broke in your code.

  ```ts
  catch (err) {
    res.status(500).json({ error: "Something went wrong" });
  }
  ```

* **502 Bad Gateway**
  One server got a bad response from another.

* **503 Service Unavailable**
  Server down or overloaded.

⚠️ Never blame the user for 5xx errors.

---

## Real MERN Stack Example

```ts
app.post("/login", async (req, res) => {
  const { email, password } = req.body;

  if (!email || !password) {
    return res.status(400).json({ message: "Missing fields" });
  }

  const user = await User.findOne({ email });
  if (!user) {
    return res.status(404).json({ message: "User not found" });
  }

  if (!isValidPassword(password)) {
    return res.status(401).json({ message: "Invalid credentials" });
  }

  res.status(200).json({ message: "Login successful" });
});
```

---

## One-Line Memory Trick 🧠⚡

* **2xx** → Success
* **3xx** → Redirect
* **4xx** → Client mistake
* **5xx** → Server mistake

---

If you want, next we can:

* Practice **status codes with Express APIs**
* Map **status codes to real-world scenarios**
* Or do a **mini quiz** to lock this into muscle memory 💪🧠

Just say the word 🚀
